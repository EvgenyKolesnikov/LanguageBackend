<UserControl x:Class="AdminClient.Views.Controls.TextControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AdminClient.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500">
    <Grid>
         <StackPanel>
            
        <StackPanel Orientation="Horizontal" Margin="0 5">
                <TextBox Width="90" Text="{Binding AddExtentedWordRequest.Word, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 5 0"/>
                <Button Width="100" Content="Добавить текст" Command="{Binding AddExtentedWordCommand}"/>
        </StackPanel>   
            
        <ListView ItemsSource="{Binding Texts}" Width="500"  Margin="0 5" HorizontalAlignment="Left">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Слово" Width="140" DisplayMemberBinding="{Binding Content }" />
                    
                    <GridViewColumn >
                        <GridViewColumn.CellTemplate >
                            <DataTemplate >
                                <Button x:Name="EditButton" Content="Select" Height="25" Width="50" Command="{Binding DataContext.GetWordByTextCommand,RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type ListView}}}" CommandParameter="{Binding RelativeSource={RelativeSource Self}}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                    <GridViewColumn >
                        <GridViewColumn.CellTemplate >
                            <DataTemplate >
                                <Button x:Name="EditButton" Content="Edit" Height="25" Width="40" Command="{Binding DataContext.OpenEditExtentedWordForm,RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type ListView}}}" CommandParameter="{Binding RelativeSource={RelativeSource Self}}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                    <GridViewColumn >
                        <GridViewColumn.CellTemplate >
                            <DataTemplate >
                                <Button x:Name="EditButton" Content="Delete" Height="25" Width="40" Command="{Binding DataContext.DeleteExtentedWordCommand,RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type ListView}}}" CommandParameter="{Binding RelativeSource={RelativeSource Self}}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                </GridView>
            </ListView.View>
        </ListView>
        
        
            
        </StackPanel>
    </Grid>
</UserControl>
